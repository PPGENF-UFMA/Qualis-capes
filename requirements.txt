# Dependências Python do Qualis-CAPES
# Instale com: pip install -r requirements.txt

# API Server (api/main.py)
fastapi>=0.137.0
uvicorn[standard]>=0.49.0
httpx>=0.28.0
pydantic>=2.13.0
python-dotenv>=1.0.0

# Compilação do banco de dados (data/compile_database.py)
pandas>=2.0.0
openpyxl>=3.1.0

# Fetch de CiteScore via API Elsevier (data/fetch_citescore.py)
requests>=2.31.0

# Scraping robusto (Latindex, fallback)
beautifulsoup4>=4.12.0

# Testes (Fase 1 — backend matching engine)
pytest>=7.4.0
